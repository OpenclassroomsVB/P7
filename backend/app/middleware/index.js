const tokenVerif = require('./token-verif')         //Token-verif import
const signupVerif = require('./signup-verif')       //Signup-verif import

module.exports = {                                  //TokenVerif & signupVerif export
    tokenVerif,
    signupVerif
}
